class Trip {
    constructor(tripId, price, schedule, ferryRoute) {
        this.tripId = tripId
        this.price = price
        this.schedule = schedule
        this.ferryRoute = ferryRoute
    }
}

module.exports = Trip