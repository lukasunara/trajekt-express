class Traveler {
    constructor(travelerId, firstName, lasName, age) {
        this.travelerId = travelerId
        this.firstName = firstName
        this.lasName = lasName
        this.age = age
    }
}

module.exports = Traveler